---
// src/pages/index.astro
---
---
const raw = Astro.request.headers.get('accept-language') ?? '';
const acceptsEn = raw.toLowerCase().includes('en');
const lang = acceptsEn ? 'en' : 'es';
return Astro.redirect(`/${lang}/`, 302);
---