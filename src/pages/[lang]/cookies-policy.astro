---
import MainLayout from '../../layouts/MainLayout.astro';
import { SITE_TITLE } from '../../consts';

const lang = Astro.params.lang;
if (lang !== 'es' && lang !== 'en') return Astro.redirect('/en/', 302);

const alternateLang = lang === 'es' ? 'en' : 'es';
const alternateLink = `/${alternateLang}/cookies-policy/`;

const title = `${lang === 'es' ? 'Política de Cookies' : 'Cookies Policy'} | ${SITE_TITLE}`;
const description = lang === 'es' ? 'Política de cookies de Slowork.' : 'Slowork cookies policy.';
---

<MainLayout
  lang={lang}
  title={title}
  description={description}
  canonical={`/${lang}/cookies-policy/`}
  headerAlternateLink={alternateLink}
  alternates={[
    { hreflang: lang, href: Astro.url.href },
    { hreflang: alternateLang, href: new URL(alternateLink, Astro.url.origin).href },
  ]}
>
  <main class="w-full max-w-5xl mx-auto px-6 pt-32 pb-16 md:py-24">
    <h1 class="font-heading font-bold text-primary text-4xl sm:text-5xl tracking-tight">
      {lang === 'es' ? 'Política de Cookies' : 'Cookies Policy'}
    </h1>
    <div class="mt-8 bg-white/70 rounded-2xl p-6 sm:p-10 border border-primary/5 shadow-sm">
      <p class="text-primary/80 leading-relaxed">
        {lang === 'es'
          ? 'Usamos cookies y tecnologías similares para mejorar la experiencia y medir el rendimiento.'
          : 'We use cookies and similar technologies to improve experience and measure performance.'}
      </p>
    </div>
  </main>
</MainLayout>

